name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  select:
    name: Tests / select suites
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      matrix: ${{ steps.selection.outputs.matrix }}
      suites: ${{ steps.selection.outputs.suites }}
      full: ${{ steps.selection.outputs.full }}
      has_suites: ${{ steps.selection.outputs.has_suites }}
      e2e_matrix: ${{ steps.selection.outputs.e2e_matrix }}
      e2e_scenarios: ${{ steps.selection.outputs.e2e_scenarios }}
      has_e2e_scenarios: ${{ steps.selection.outputs.has_e2e_scenarios }}
    steps:
      - name: Checkout comparison history
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Validate suite manifest
        run: python3 tools/testing/validate_manifest.py

      - name: Test suite selector
        run: python3 -m unittest discover -s tools/testing -p 'test_*.py' -v

      - name: Select suites
        id: selection
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          BEFORE_SHA: ${{ github.event.before }}
          PUSH_SHA: ${{ github.sha }}
        run: |
          case "$EVENT_NAME" in
            pull_request)
              python3 tools/testing/select_suites.py \
                --base "$BASE_SHA" \
                --head "$HEAD_SHA" \
                --github-output "$GITHUB_OUTPUT"
              ;;
            push)
              python3 tools/testing/select_suites.py \
                --base "$BEFORE_SHA" \
                --head "$PUSH_SHA" \
                --github-output "$GITHUB_OUTPUT"
              ;;
            workflow_dispatch)
              python3 tools/testing/select_suites.py \
                --files tests/manifest.json tests/e2e_manifest.json \
                --github-output "$GITHUB_OUTPUT"
              ;;
            *)
              echo "Unsupported event: $EVENT_NAME" >&2
              exit 1
              ;;
          esac

  build-tests:
    name: Tests / build gameplay ROMs
    needs: [select]
    if: ${{ needs.select.result == 'success' && needs.select.outputs.has_suites == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      MGBA_REVISION: 26b7884bc25a5933960f3cdcd98bac1ae14d42e2
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Checkout pinned mGBA
        uses: actions/checkout@v6
        with:
          repository: mgba-emu/mgba
          ref: ${{ env.MGBA_REVISION }}
          path: mgba

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-arm-none-eabi binutils-arm-none-eabi libpng-dev cmake

      - name: Build test ROMs and mGBA runner
        run: |
          make -j"$(nproc)" test-roms
          cmake -S mgba -B mgba/build \
            -DBUILD_ROM_TEST=ON -DBUILD_QT=OFF -DBUILD_SDL=OFF
          cmake --build mgba/build --target mgba-rom-test -j"$(nproc)"
          mkdir -p build/test/runtime
          cp -L mgba/build/libmgba.so.0.10 build/test/runtime/

      - name: Upload test bundle
        uses: actions/upload-artifact@v7
        with:
          name: gameplay-test-bundle
          path: |
            build/test/roms/
            build/test/runtime/
            mgba/build/test/mgba-rom-test
          retention-days: 1

  run-suites:
    name: Tests / ${{ matrix.suite }}
    needs: [select, build-tests]
    if: ${{ needs.select.outputs.has_suites == 'true' && needs.build-tests.result == 'success' }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.select.outputs.matrix) }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout runner
        uses: actions/checkout@v6

      - name: Download test bundle
        uses: actions/download-artifact@v8
        with:
          name: gameplay-test-bundle

      - name: Run selected suite
        env:
          MGBA_ROM_TEST: ${{ github.workspace }}/mgba/build/test/mgba-rom-test
          LD_LIBRARY_PATH: ${{ github.workspace }}/build/test/runtime
        run: |
          chmod +x "$MGBA_ROM_TEST"
          python3 tools/testing/run_tests.py --tests "${{ matrix.suite }}"

      - name: Upload suite diagnostics
        if: ${{ failure() }}
        uses: actions/upload-artifact@v7
        with:
          name: gameplay-test-failure-${{ matrix.suite }}
          path: |
            build/test/artifacts/${{ matrix.suite }}/
            build/test/roms/${{ matrix.suite }}.elf
            build/test/roms/${{ matrix.suite }}.map
            build/test/roms/${{ matrix.suite }}.sym
          retention-days: 1

  run-e2e:
    name: Tests / E2E / ${{ matrix.scenario }}
    needs: [select]
    if: ${{ needs.select.result == 'success' && needs.select.outputs.has_e2e_scenarios == 'true' }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.select.outputs.e2e_matrix) }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      MGBA_REVISION: 26b7884bc25a5933960f3cdcd98bac1ae14d42e2
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Checkout pinned mGBA
        uses: actions/checkout@v6
        with:
          repository: mgba-emu/mgba
          ref: ${{ env.MGBA_REVISION }}
          path: mgba

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential gcc-arm-none-eabi binutils-arm-none-eabi libpng-dev cmake

      - name: Build pinned mGBA library
        run: |
          cmake -S mgba -B mgba/build \
            -DBUILD_QT=OFF -DBUILD_SDL=OFF -DBUILD_GLES2=OFF \
            -DBUILD_GL=OFF -DBUILD_EGL=OFF
          cmake --build mgba/build -j"$(nproc)"

      - name: Run selected E2E scenario
        env:
          E2E_COMMIT_SHA: ${{ github.sha }}
          LD_LIBRARY_PATH: ${{ github.workspace }}/mgba/build
        run: make -j"$(nproc)" e2e MGBA_DIR=mgba TESTS="${{ matrix.scenario }}"

      - name: Upload filtered E2E diagnostics
        if: ${{ failure() }}
        uses: actions/upload-artifact@v7
        with:
          name: e2e-failure-${{ matrix.scenario }}
          path: build/e2e/upload/${{ matrix.scenario }}/
          if-no-files-found: error
          retention-days: 1

  cleanup-artifacts:
    name: Tests / delete test artifacts
    if: ${{ always() }}
    needs: [build-tests, run-suites, run-e2e]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      actions: write
    steps:
      - name: Delete artifacts from this workflow run
        env:
          GH_TOKEN: ${{ github.token }}
          RUN_ARTIFACTS_URL: ${{ github.api_url }}/repos/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts
        run: |
          artifact_list="$(mktemp)"
          curl --fail-with-body --silent --show-error \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "$RUN_ARTIFACTS_URL?per_page=100" > "$artifact_list"
          while IFS= read -r artifact_id; do
            curl --fail-with-body --silent --show-error \
              -X DELETE \
              -H "Accept: application/vnd.github+json" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "X-GitHub-Api-Version: 2022-11-28" \
              "${{ github.api_url }}/repos/${{ github.repository }}/actions/artifacts/$artifact_id"
          done < <(jq -r '.artifacts[].id' "$artifact_list")

  no-suites:
    name: Tests / no gameplay suites
    needs: [select]
    if: ${{ needs.select.result == 'success' && needs.select.outputs.has_suites == 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo "No gameplay suites selected; documentation-only gate passes."

  no-e2e-scenarios:
    name: Tests / no E2E scenarios
    needs: [select]
    if: ${{ needs.select.result == 'success' && needs.select.outputs.has_e2e_scenarios == 'false' }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo "No E2E scenarios selected; E2E gate passes."

  required:
    name: Tests / required
    if: ${{ always() }}
    needs: [select, build-tests, run-suites, no-suites, run-e2e, no-e2e-scenarios, cleanup-artifacts]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout gate verifier
        uses: actions/checkout@v6

      - name: Verify test workflow results
        env:
          SELECT_RESULT: ${{ needs.select.result }}
          BUILD_RESULT: ${{ needs.build-tests.result }}
          RUN_RESULT: ${{ needs.run-suites.result }}
          NO_SUITES_RESULT: ${{ needs.no-suites.result }}
          CLEANUP_RESULT: ${{ needs.cleanup-artifacts.result }}
          E2E_RESULT: ${{ needs.run-e2e.result }}
          NO_E2E_RESULT: ${{ needs.no-e2e-scenarios.result }}
          HAS_SUITES: ${{ needs.select.outputs.has_suites }}
          SELECTED_SUITES: ${{ needs.select.outputs.suites }}
          HAS_E2E_SCENARIOS: ${{ needs.select.outputs.has_e2e_scenarios }}
          SELECTED_E2E_SCENARIOS: ${{ needs.select.outputs.e2e_scenarios }}
        run: |
          echo "Selected gameplay suites: $SELECTED_SUITES"
          echo "Selected E2E scenarios: $SELECTED_E2E_SCENARIOS"
          python3 tools/testing/verify_required.py
